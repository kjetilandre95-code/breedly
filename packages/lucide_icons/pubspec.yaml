name: lucide_icons
description: Local compatibility shim for Lucide icon references.
version: 0.257.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
